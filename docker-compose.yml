version: '3.8'

services:
  web:
    build: .
    container_name: drrivka-medical-site
    ports:
      - "3000:80"
    environment:
      - NGINX_HOST=localhost
      - NGINX_PORT=80
    restart: always
